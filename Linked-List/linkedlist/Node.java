package linkedlist;

public class Node {
    int data; // the data is being stored inside the linkedlist
    Node next; // this is an empty object which has null as the value
    Node(int data){
        this.data = data;
    }
}
